-Properties는 Hashtable의 하위 클래스이다.
-Properties는 Hashtable과 달리 키와 값을 String 타입으로 받는다.
-Properties는 애플리케이션 옵션정보, DB 연결정보, 국제화 정보가 정보과 저장된 프로퍼티 파일(~.properties)을 읽을 때 주로 사용

-프로퍼티 파일은 Properties 객체 생성 후, load() 메소드 호출하여 읽을 수 있다.

